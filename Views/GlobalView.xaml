<Window x:Class="PosApp.Views.GlobalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PosApp.Views"
        xmlns:viewModel="clr-namespace:PosApp.ViewModels"
        mc:Ignorable="d"
        Title="GlobalView" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="true">

    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:LoginViewModel}">
            <local:LoginView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:ServerSettingViewModel}">
            <local:ServerSettingView/>
        </DataTemplate>

    </Window.Resources>

    <Grid>
        
        <ContentControl Content="{Binding CurrentChildView}"></ContentControl>
        <!--<Button Width="20" Height="20"  Content="test" Command="{Binding ShowDashboardView}"></Button>-->
    </Grid>
</Window>
